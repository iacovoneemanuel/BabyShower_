<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gender Reveal 💖💙 Elisabetta & Emanuel</title>

    <meta property="og:title" content="Gender Reveal - Elisabetta & Emanuel 🧸">
    <meta property="og:description" content="Ti aspettiamo il 4 Aprile! Sarà maschietto o femminuccia? 🎀💙">

    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;600&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/canvas-confetti@1.6.0/dist/confetti.browser.min.js"></script>

    <style>
        :root {
            --pink: #ffb7e0;
            --blue: #a3d8ff;
            --text: #5a5a5a;
        }

        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            font-family: 'Quicksand', sans-serif;
            text-align: center;
            color: var(--text);
            overflow: hidden;
            background: #fdfbfb;
        }

        /* --- Pagina di Copertina --- */
        #cover {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle, #ffffff 0%, #ffe9f5 50%, #e3f2ff 100%);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            z-index: 1000;
            transition: opacity 0.8s ease;
        }

        .cover-balloons {
            font-size: 110px;
            margin-bottom: 20px;
        }

        .balloon-pop {
            display: inline-block;
            cursor: pointer;
            transition: transform 0.3s;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .bounce { animation: bounce 2s infinite ease-in-out; }

        #entry-button {
            background: linear-gradient(45deg, var(--pink), var(--blue));
            color: white;
            border: 4px solid white;
            padding: 20px 50px;
            font-size: 20px;
            font-weight: 600;
            cursor: pointer;
            border-radius: 50px;
            box-shadow: 0 8px 20px rgba(0,0,0,0.1);
        }

        /* --- Contenuto Principale --- */
        #main-content {
            opacity: 0;
            visibility: hidden;
            background: linear-gradient(180deg, #fff5f9 0%, #f0f7ff 100%);
            min-height: 100%;
            overflow-y: auto;
            padding-bottom: 50px;
        }

        .container {
            padding: 30px 20px;
            max-width: 500px;
            margin: 0 auto;
        }

        .card {
            background: white;
            padding: 30px;
            margin: 25px auto;
            border-radius: 40px;
            box-shadow: 0 15px 0px rgba(0,0,0,0.02);
            border: 3px solid #f0f0f0;
            position: relative;
        }

        h1 { font-size: 28px; color: #777; margin-bottom: 10px; }
        h2 { font-size: 22px; color: #888; }

        /* Icone decorative nelle card */
        .decor {
            font-size:
